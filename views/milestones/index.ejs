<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Ella Rises</title>
  <link rel="icon" type="image/png" href="/images/EllaRisesHeart.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      margin-left: 1rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-header h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 2.5rem;
      color: var(--primary);
      margin: 0;
    }

    .table-container {
      background: transparent;
      padding: 0;
      border-radius: 0;
      overflow-x: auto;
      margin: 0 1rem;
      width: calc(100% - 2rem);
    }

    .search-container {
      margin: 0 1rem 1rem 1rem;
      display: flex;
      gap: 0.75rem;
      align-items: stretch;
      flex-wrap: nowrap;
      box-sizing: border-box;
      width: calc(100% - 2rem);
      padding: 0;
      background: transparent;
      border: none;
    }

    .search-container input[type="text"] {
      flex: 1;
      min-width: 150px;
      padding: 0.75rem;
      border: 2px solid var(--pink-light);
      border-radius: 8px;
      font-size: 1rem;
      box-sizing: border-box;
      height: 48px;
    }

    .search-container select {
      padding: 0.75rem;
      border: 2px solid var(--pink-light);
      border-radius: 8px;
      min-width: 200px;
      max-width: 300px;
      font-size: 1rem;
      box-sizing: border-box;
      flex-shrink: 0;
      height: 48px;
    }

    .search-container form {
      display: contents !important;
      margin: 0 !important;
      padding: 0 !important;
      border: none !important;
      background: transparent !important;
      box-shadow: none !important;
      outline: none !important;
    }

    .search-container button[type="submit"] {
      padding: 0 1.5rem;
      font-size: 1rem;
      font-weight: 600;
      white-space: nowrap;
      margin: 0;
      flex-shrink: 0;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 48px;
      border: none !important;
      border-radius: 8px;
      line-height: 1;
      outline: none !important;
      box-shadow: none !important;
      background: var(--secondary) !important;
      color: white !important;
    }

    .table-container table {
      margin-top: 0;
      background: white;
    }

    .action-buttons .btn-small {
      padding: 0.4rem 0.8rem;
      font-size: 0.8rem;
      height: auto;
      line-height: 1.5;
    }

  </style>
</head>
<body>
<%- include('../partials/header') %>
<main class="container" style="padding-top: 2rem;">
  <div class="page-header">
    <h1>Participant Milestones</h1>
  </div>

  <div class="search-container">
    <input type="text" name="search" placeholder="Search by participant or milestone..." value="<%= typeof search !== 'undefined' ? search : '' %>" form="search-form">
    <select name="type" form="search-form">
      <option value="">All Types</option>
      <% if (typeof milestoneTypes !== 'undefined' && milestoneTypes && milestoneTypes.length > 0) { %>
        <% milestoneTypes.forEach(type => { %>
          <option value="<%= type %>" <%= (typeof selectedType !== 'undefined' && selectedType === type) ? 'selected' : '' %>><%= type %></option>
        <% }) %>
      <% } %>
    </select>
    <form method="GET" id="search-form" style="display: contents; margin: 0; padding: 0; border: none; background: transparent; box-shadow: none; outline: none;">
      <button type="submit">Search</button>
    </form>
  </div>

  <div class="table-container">
    <table>
      <thead>
        <tr>
          <th>Date Achieved</th>
          <th>Participant</th>
          <th>Milestone</th>
          <% if (isManager) { %>
            <th>Actions</th>
          <% } %>
        </tr>
      </thead>
      <tbody>
        <% if (milestones.length === 0) { %>
          <tr><td colspan="<%= isManager ? 4 : 3 %>" style="text-align: center;">No milestones found</td></tr>
        <% } else { %>
          <% milestones.forEach(milestone => { %>
            <tr>
              <td><%= milestone.milestone_date ? new Date(milestone.milestone_date).toLocaleDateString() : 'N/A' %></td>
              <td><%= milestone.first_name %> <%= milestone.last_name %></td>
              <td><%= milestone.milestone_name %></td>
              <% if (isManager) { %>
                <td>
                  <div class="action-buttons">
                    <form action="/milestones/<%= milestone.milestone_id %>/delete" method="POST" style="display: inline; margin: 0; padding: 0;" onsubmit="return confirm('Are you sure you want to delete this milestone?')">
                      <button type="submit" class="btn-small btn-delete">Delete</button>
                    </form>
                  </div>
                </td>
              <% } %>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</main>
<%- include('../partials/footer') %>
<script src="/js/main.js"></script>
</body>
</html>
