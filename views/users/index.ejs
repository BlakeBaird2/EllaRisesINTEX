<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Ella Rises</title>
  <link rel="icon" type="image/png" href="/images/EllaRisesHeart.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      margin-left: 1rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-header h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 2.5rem;
      color: var(--primary);
      margin: 0;
    }

    .search-container {
      margin: 0 1rem 1rem 1rem;
      display: flex;
      gap: 0.75rem;
      align-items: stretch;
      flex-wrap: nowrap;
      box-sizing: border-box;
      width: calc(100% - 2rem);
      padding: 0;
      background: transparent;
      border: none;
    }

    .search-container input[type="text"] {
      flex: 1;
      min-width: 150px;
      padding: 0.75rem;
      border: 2px solid var(--pink-light);
      border-radius: 8px;
      font-size: 1rem;
      box-sizing: border-box;
      height: 48px;
    }

    .search-container select {
      padding: 0.75rem;
      border: 2px solid var(--pink-light);
      border-radius: 8px;
      min-width: 120px;
      max-width: 180px;
      font-size: 1rem;
      box-sizing: border-box;
      flex-shrink: 0;
      height: 48px;
    }

    .search-container form {
      display: contents !important;
      margin: 0 !important;
      padding: 0 !important;
      border: none !important;
      background: transparent !important;
      box-shadow: none !important;
      outline: none !important;
    }

    .search-container button[type="submit"] {
      padding: 0 1.5rem;
      font-size: 1rem;
      font-weight: 600;
      white-space: nowrap;
      margin: 0;
      flex-shrink: 0;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 48px;
      border: none !important;
      border-radius: 8px;
      line-height: 1;
      outline: none !important;
      box-shadow: none !important;
      background: var(--secondary) !important;
      color: white !important;
    }

    .search-container .btn-add {
      margin-left: auto;
      padding: 0 1.5rem;
      font-size: 1rem;
      font-weight: 600;
      white-space: nowrap;
      margin-top: 0;
      margin-bottom: 0;
      margin-right: 0;
      flex-shrink: 0;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 48px;
      border: none !important;
      border-radius: 8px;
      line-height: 1;
      outline: none !important;
      box-shadow: none !important;
      background: var(--secondary) !important;
      color: white !important;
      text-decoration: none;
    }

    .search-container select {
      padding: 0.75rem;
      border: 2px solid var(--pink-light);
      border-radius: 8px;
      min-width: 180px;
      max-width: 250px;
      font-size: 1rem;
      box-sizing: border-box;
      flex-shrink: 0;
      height: 48px;
    }

    .search-container form {
      display: contents !important;
      margin: 0 !important;
      padding: 0 !important;
      border: none !important;
      background: transparent !important;
      box-shadow: none !important;
      outline: none !important;
    }

    .search-container button[type="submit"] {
      padding: 0 1.5rem;
      font-size: 1rem;
      font-weight: 600;
      white-space: nowrap;
      margin: 0;
      flex-shrink: 0;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 48px;
      border: none !important;
      border-radius: 8px;
      line-height: 1;
      outline: none !important;
      box-shadow: none !important;
      background: var(--secondary) !important;
      color: white !important;
    }

    .table-container {
      background: transparent;
      padding: 0;
      border-radius: 0;
      overflow-x: auto;
      margin: 0 1rem;
      width: calc(100% - 2rem);
    }

    .table-container table {
      margin-top: 0;
      background: white;
    }

    .action-buttons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: nowrap;
      align-items: center;
      white-space: nowrap;
    }

    .action-buttons .btn-small {
      padding: 0.4rem 0.8rem;
      font-size: 0.8rem;
      height: auto;
      line-height: 1.5;
    }
  </style>
</head>
<body>
<%- include('../partials/header') %>
<main class="container" style="padding-top: 2rem;">
  <div class="page-header">
    <h1>User Management</h1>
  </div>

  <div class="search-container">
    <form method="GET" id="search-form" style="display: contents; margin: 0; padding: 0; border: none; background: transparent; box-shadow: none; outline: none;">
      <input type="text" name="search" placeholder="Search by username, email, or name..." value="<%= typeof search !== 'undefined' ? search : '' %>">
      <select name="role">
        <option value="">All Roles</option>
        <option value="user" <%= (typeof selectedRole !== 'undefined' && selectedRole === 'user') ? 'selected' : '' %>>User</option>
        <option value="admin" <%= (typeof selectedRole !== 'undefined' && selectedRole === 'admin') ? 'selected' : '' %>>Admin</option>
      </select>
      <button type="submit">Search</button>
    </form>
    <a href="/users/new" class="btn-add">+ Add New User</a>
  </div>

  <div class="table-container">
    <table>
      <thead>
        <tr>
          <th>Username</th>
          <th>Email</th>
          <th>Role</th>
          <th>Active</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if (users.length === 0) { %>
          <tr><td colspan="5" style="text-align: center;">No users found</td></tr>
        <% } else { %>
          <% users.forEach(u => { %>
            <tr>
              <td><%= u.username %></td>
              <td><%= u.email %></td>
              <td><%= u.role %></td>
              <td><%= u.is_active ? 'Yes' : 'No' %></td>
              <td>
                <div class="action-buttons">
                  <a href="/users/<%= u.user_id %>/edit" class="btn-small btn-edit">Edit</a>
                  <form action="/users/<%= u.user_id %>/delete" method="POST" style="display: inline; margin: 0; padding: 0;" onsubmit="return confirm('Are you sure you want to delete this user?')">
                    <button type="submit" class="btn-small btn-delete">Delete</button>
                  </form>
                </div>
              </td>
            </tr>
          <% }); %>
        <% } %>
      </tbody>
    </table>
  </div>
</main>
<%- include('../partials/footer') %>
<script src="/js/main.js"></script>
</body>
</html>
