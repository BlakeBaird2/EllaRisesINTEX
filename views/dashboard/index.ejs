<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Ella Rises</title>
  <link rel="icon" type="image/png" href="/images/EllaRisesHeart.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .dashboard-section {
      margin-bottom: 3rem;
    }

    .section-title {
      font-family: 'DM Serif Display', serif;
      font-size: 2rem;
      color: var(--primary);
      text-align: left;
      margin-bottom: 1rem;
      margin-left: 1.5rem;
      font-weight: 400;
    }

    .table-container {
      background: var(--pink-light);
      padding: 2rem;
      border-radius: 12px;
      margin-left: 1rem;
      margin-right: 1rem;
      overflow-x: auto;
    }

    .table-container table {
      margin-top: 0;
      background: white;
    }

    .action-buttons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .btn-small {
      padding: 0.4rem 0.8rem;
      font-size: 0.85rem;
      text-decoration: none;
      border-radius: 6px;
      display: inline-block;
      transition: all 0.3s;
    }

    .btn-edit {
      background: var(--secondary);
      color: white;
    }

    .btn-edit:hover {
      background: #89a48b;
    }

    .btn-delete {
      background: var(--accent);
      color: white;
    }

    .btn-delete:hover {
      background: #b02a4d;
    }

    .btn-add {
      background: var(--secondary);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      text-decoration: none;
      display: inline-block;
      font-weight: 600;
      margin-left: 1.5rem;
      margin-bottom: 1rem;
    }

    .btn-add:hover {
      background: #89a48b;
      transform: translateY(-2px);
    }

    .truncate {
      max-width: 200px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .metrics {
      margin-bottom: 3rem;
    }
  </style>
</head>
<body>
<%- include('../partials/header') %>
<main class="container" style="padding-top: 2rem;">
  <h1 style="font-family: 'DM Serif Display', serif;">Dashboard</h1>

  <div class="metrics">
    <div class="metric"><h3><%= metrics.participants %></h3><p>Participants</p></div>
    <div class="metric"><h3><%= metrics.events %></h3><p>Events</p></div>
    <div class="metric"><h3><%= metrics.surveys %></h3><p>Surveys</p></div>
    <div class="metric"><h3><%= metrics.milestones %></h3><p>Milestones</p></div>
    <div class="metric"><h3>$<%= metrics.totalDonations.toFixed(2) %></h3><p>Total Donations</p></div>
  </div>

  <% if (tableauUrl) { %>
  <div class="tableau-embed">
    <h2>Program Impact Analytics</h2>
    <iframe src="<%= tableauUrl %>" width="100%" height="800"></iframe>
  </div>
  <% } %>
</main>
<%- include('../partials/footer') %>
<script src="/js/main.js"></script>
</body>
</html>
